name: Test Action

on:
  push:
    branches:
      - nadaaa 

jobs:

  hello_world:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      
      - uses: actions/setup-node@v2
      
      - name: Install depenedencies
        run : npm ci
